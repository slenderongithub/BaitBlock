# Data Flow

## End-to-End (Live Node Backend)

```mermaid
sequenceDiagram
    participant B as Browser
    participant S as Express (src/server.js)
    participant T as Target Article Site

    B->>S: POST /api/analyze { url }
    S->>S: new URL(url) validation
    S->>T: fetch(url, User-Agent spoofed)
    T-->>S: HTML
    S->>S: cheerio.load(html)
    S->>S: parseJsonLdNodes()
    S->>S: extractTitle() / extractBodyText() (fallback chain)
    S->>S: extractMetaDescription() / extractPublishedAt() / extractAuthors()
    S->>S: computeScore(title, bodyText)
    S-->>B: JSON { verdict, score, signals, score_breakdown, ... }
    B->>B: normalizeApiResponse() + renderResult()
```

## Title Extraction Fallback Chain

`og:title` meta → `twitter:title` meta → first `<h1>` text → `<title>` tag text → (first non-empty wins).

## Body Extraction Fallback Chain

1. Prune noisy DOM nodes: `script`, `style`, `nav`, `footer`, `header`, `aside`, ad/cookie/consent/analytics/tracking/share/related/comment selectors.
2. Try JSON-LD `articleBody` from any `NewsArticle`/`Article`/`Report`-typed node — used if length > 220 chars and not "noisy."
3. Try a ranked list of CSS selectors (`article p`, `main p`, `[role='main'] p`, `.article p`, `.post-content p`, `.entry-content p`, `.story-content p`, `.content p`), keeping paragraphs that pass `isReadableParagraph` (≥60 chars, ≥10 words, ≤2 excessively long "words", not boilerplate like "cookie"/"subscribe"/"privacy policy"). Accepts the first selector whose combined text is ≥80 tokens and >200 chars.
4. Fallback: all `<p>` tags on the page, same readability filter, first 20, if combined length > 200 chars.
5. Final fallback: same scan scoped to `<body>` only; if still noisy, body text is empty and the API surfaces `"No readable article body found"` as the extraction method.

Every step records which method succeeded (`extraction_method` in the response) — this is one of the more useful debugging fields in the payload.

## Scoring Data Flow

```mermaid
flowchart TD
    T[title] --> BP[BAIT_PATTERNS regex scan]
    T --> DH[DECEPTION_HINTS regex scan]
    T --> PUNC[Punctuation + uppercase ratio]
    T --> LEN[Headline length check]
    T --> ST[computeLexicalSimilarity title,bodyText]
    T --> SP[computeSentimentPolarity title]
    ST -->|overlap ratio < 0.35| GAP[semanticGap = true]
    SP -->|abs value > 0.5| TONE[sensationalTone = true]
    BP --> SUM[score accumulation, clamped 0-100]
    DH --> SUM
    PUNC --> SUM
    LEN --> SUM
    GAP --> SUM
    TONE --> SUM
    GAP -->|both true| SYN[+6 synergy bonus]
    TONE --> SYN
    SUM --> VERDICT["bucket: risky/warning/safe → verdict string"]
```

`computeLexicalSimilarity` is **not** semantic similarity despite the field name `cosine_similarity_score` — it's a plain set-intersection ratio of non-stopword tokens between headline and body. The Python backend (`app.py`) computes a *real* cosine similarity over sentence embeddings for the same field name, which means the two backends' `cosine_similarity_score` values are not comparable even though they share a field name and threshold (0.35).

## Data Flow — Python Backend (for reference; not currently reachable, see [[Known-Issues]])

```mermaid
flowchart LR
    U[URL] --> SC{scrape_article}
    SC -->|try| NP[newspaper3k Article.download/parse]
    SC -->|on failure or thin body| BS[BeautifulSoup4 fallback]
    NP --> H[headline, body, authors, published_at]
    BS --> H
    H --> PRE[preprocess_text: spaCy lemmatize, strip stopwords/punct]
    PRE --> EMB[SentenceTransformer encode headline+body]
    EMB --> COS[cosine_similarity]
    H --> VADER[SentimentIntensityAnalyzer.polarity_scores]
    COS --> COMP[compute_composite: weighted score + verdict]
    VADER --> COMP
    H --> NER[spaCy doc.ents grouped by label]
    H --> KP[key_phrases_from_body via Counter]
    COMP --> RESP[JSON response]
    NER --> RESP
    KP --> RESP
```

## Evaluation/Chart Data Flow (disconnected side pipeline)

```mermaid
flowchart LR
    CSV[Hand-authored CSVs: results.csv, confusion_matrix.csv, ...] --> PLOT[plot_*.py scripts]
    PLOT --> FIG[fig1..fig7 PNGs]
    FIG --> README[Embedded in README.md]
```

No arrow exists from either live server into this pipeline — the CSVs are not generated by running the app.
